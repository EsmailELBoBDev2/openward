{
  "name": "openward",
  "version": "0.0.0",
  "private": true,
  "description": "OpenSmile — a FOSS dental-clinic management system (bilingual EN/AR; odontogram, treatment plans, appointments, billing, recalls, a prescribing safety guard, and a patient portal). Node unit tests run against the bundled vendor/sql-wasm.js and the browser globals Node provides (crypto.subtle, TextEncoder); no dependencies.",
  "scripts": {
    "preflight": "node tools/preflight.js",
    "pretest": "node tools/preflight.js",
    "test": "node test/run-all.js"
  }
}
